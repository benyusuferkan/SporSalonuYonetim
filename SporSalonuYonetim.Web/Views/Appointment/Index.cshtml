@model IEnumerable<SporSalonuYonetim.Web.Entities.Appointment>

@{
    ViewData["Title"] = "Randevularım";
}

<div class="container mt-4">
    
    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="fa fa-check-circle"></i> @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }
    @if (TempData["InfoMessage"] != null)
    {
        <div class="alert alert-info alert-dismissible fade show" role="alert">
            <i class="fa fa-info-circle"></i> @TempData["InfoMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }
    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <i class="fa fa-exclamation-triangle"></i> @TempData["ErrorMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>📅 Randevularım</h2>
        <a asp-action="Create" class="btn btn-primary shadow">
            <i class="fa fa-plus"></i> Yeni Randevu Al
        </a>
    </div>

    @if (!Model.Any())
    {
        <div class="alert alert-warning text-center">
            <h4>Henüz hiç randevunuz yok.</h4>
            <p>Hemen sağ üstteki butondan yeni bir antrenman planlayın!</p>
        </div>
    }
    else
    {
        <div class="card shadow-sm border-0">
            <div class="card-body p-0">
                <table class="table table-hover table-striped mb-0">
                    <thead class="bg-light">
                        <tr>
                            <th>Tarih & Saat</th>
                            <th>Hizmet</th>
                            <th>Eğitmen</th>
                            <th>Durum</th>
                            <th class="text-center">İşlemler</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="align-middle">
                                    <span class="fw-bold text-primary">@item.AppointmentDate.ToString("dd.MM.yyyy")</span>
                                    <br />
                                    <small class="text-muted">@item.AppointmentDate.ToString("HH:mm")</small>
                                </td>
                                <td class="align-middle">@item.Service?.Name</td>
                                <td class="align-middle">
                                    <div class="d-flex align-items-center">
                                        @if (!string.IsNullOrEmpty(item.Trainer?.PhotoUrl))
                                        {
                                            <img src="~/images/@item.Trainer.PhotoUrl" class="rounded-circle me-2" width="40" height="40" style="object-fit:cover;" />
                                        }
                                        <span>@item.Trainer?.FullName</span>
                                    </div>
                                </td>
                                <td class="align-middle">
                                    @if (item.Status == "Onaylandı")
                                    {
                                        <span class="badge bg-success">Onaylandı</span>
                                    }
                                    else if (item.Status == "Reddedildi")
                                    {
                                        <span class="badge bg-danger">Reddedildi</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-secondary">Beklemede</span>
                                    }
                                </td>
                                <td class="align-middle text-center">
                                    @if (item.IsCancellationRequested)
                                    {
                                        <span class="badge bg-warning text-dark border border-warning p-2">
                                            <i class="fa fa-clock"></i> İptal Onayı Bekleniyor
                                        </span>
                                    }
                                    else
                                    {
                                        <form asp-action="RequestCancellation" method="post" style="display:inline;">
                                            <input type="hidden" name="id" value="@item.Id" />
                                            <button type="submit" class="btn btn-sm btn-outline-danger" 
                                                    onclick="return confirm('Bu randevuyu iptal etmek istediğinize emin misiniz? Yönetici onayından sonra silinecektir.');">
                                                <i class="fa fa-times"></i> İptal Talebi Oluştur
                                            </button>
                                        </form>
                                    }
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    }
</div>