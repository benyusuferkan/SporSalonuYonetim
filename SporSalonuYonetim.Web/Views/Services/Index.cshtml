@model IEnumerable<SporSalonuYonetim.Web.Entities.Service>

@{
    ViewData["Title"] = "Hizmetler";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2>🏋️ Hizmet Yönetimi</h2>
        <a asp-action="Create" class="btn btn-success">+ Yeni Hizmet Ekle</a>
    </div>

    <table class="table table-hover table-bordered shadow-sm">
        <thead class="table-dark">
            <tr>
                <th>Hizmet Adı</th>
                <th>Süre (Dk)</th>
                <th>Ücret (TL)</th>
                <th>Açıklama</th>
                <th>İşlemler</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td class="fw-bold">@item.Name</td>
                    <td>@item.Duration dk</td>
                    <td>@item.Price ₺</td>
                    <td>@item.Description</td>
                    <td>
                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger" 
                           onclick="return confirm('Bu hizmeti silmek istediğine emin misin?');">Sil</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>